node_modules
lib/reference
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Local development logs
vite.log
*.log
cloudflared.exe
# Environment variables (CRITICAL: Do not commit secrets!)
.env
.env.local
.env.*.local

# Claude Code local state — runtime scratch, not source
.claude/scheduled_tasks.lock
.claude/worktrees/

# Backup files left behind during refactors
*.backup
*.bak
